# Purpose
This is a github-action function that is returning next avalible version to use in casual while building.

# Usage
```
      - uses: casualcore/casual-version
        id: version
        with:
          build_type: alpha
